import 'package:flutter/material.dart';

class MobileLabsScreen extends StatelessWidget {
  const MobileLabsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("MobileLabsScreen"),
    );
  }
}