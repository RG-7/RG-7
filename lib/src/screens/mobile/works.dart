import 'package:flutter/material.dart';

class MobileWorksScreen extends StatelessWidget {
  const MobileWorksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("MobileWorksScreen"),
    );
  }
}
