import 'package:flutter/material.dart';

class MobileExperienceScreen extends StatelessWidget {
  const MobileExperienceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("MobileExperienceScreen"),
    );
  }
}