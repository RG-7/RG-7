import 'package:flutter/material.dart';

class MobileServiceScreen extends StatelessWidget {
  const MobileServiceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("MobileServiceScreen"),
    );
  }
}
