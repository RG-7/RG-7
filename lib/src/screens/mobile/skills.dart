import 'package:flutter/material.dart';

class MobileSkillsScreen extends StatelessWidget {
  const MobileSkillsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("MobileSkillsScreen"),
    );
  }
}