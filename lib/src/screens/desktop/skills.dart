import 'package:flutter/material.dart';

class DesktopSkillsScreen extends StatelessWidget {
  const DesktopSkillsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("DesktopSkillsScreen"),
    );
  }
}