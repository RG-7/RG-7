import 'package:flutter/material.dart';

class DesktopWorksScreen extends StatelessWidget {
  const DesktopWorksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("DesktopWorksScreen"),
    );
  }
}