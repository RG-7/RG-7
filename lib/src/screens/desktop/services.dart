import 'package:flutter/material.dart';

class DesktopServiceScreen extends StatelessWidget {
  const DesktopServiceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("DesktopServiceScreen"),
    );
  }
}
