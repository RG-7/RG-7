import 'package:flutter/material.dart';

class DesktopLabsScreen extends StatelessWidget {
  const DesktopLabsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("DesktopLabsScreen"),
    );
  }
}